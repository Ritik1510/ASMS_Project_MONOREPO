# ASMS_Project_MONOREPO/ce-server/ .... 
This is the main project file that describes the `postgresSql sql based db` side of `ASMS_PROJECT`. 
- That is completely transformed in non-relational database, in `MONGO_DB`
  - Watch it on atlas platfrom provided by mongo_Db.